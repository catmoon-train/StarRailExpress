# 2026-06-20 工作日志

## HudStoreRenderer - 代币显示位置调整

修改了 `src/main/java/io/wifi/starrailexpress/client/gui/HudStoreRenderer.java`：

1. **MoneyNumberRenderer 新增 getWidth() 方法**：计算金币总宽度（图标宽度 + 数字位数 × 8）
2. **代币渲染位置调整**：
   - 有金币时：代币显示在金币左边同一水平线（Y=6），间距 8px
   - 无金币时：代币显示在右上角（Y=6）
3. **构建验证**：`./gradlew build -x test` 通过，无编译错误
